import '@testing-library/jest-dom'

import CustomersHeader from '../app/ui/customersHeader';

describe("CustomersHeader tests", () => {
  it("should execute without error", () => {
    <CustomersHeader />
  });
});
