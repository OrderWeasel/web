import '@testing-library/jest-dom'

import MerchantsHeader from '../app/ui/merchantsHeader';

describe("MerchantsHeader tests", () => {
  it("should execute without error", () => {
    <MerchantsHeader />
  });
});
