import '@testing-library/jest-dom'

import Confirmation from '../app/(customers)/restaurants/checkout/confirmation/page';

describe("Confirmation tests", () => {
  it("should execute without error", () => {
    <Confirmation />
  });
});
