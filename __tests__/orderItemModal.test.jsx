import '@testing-library/jest-dom'

import OrderItemModal from '../app/ui/orderItemModal'

describe("OrderItemModal tests", () => {
  it("should execute without error", () => {
    <OrderItemModal />
  });
});
